# Contexto de trabajo de Claude — NO desplegar a la web viva
CLAUDE.md
CONTEXTO_CONVERSACION.md

# Imágenes y referencias UX (terceros) — solo para diseño, NO desplegar
images_ref/
ux_ref/

# Guía interna de plantilla de servicios — NO desplegar a la web
docs/

# Overrides personales del detector de diseno (la config compartida SI se versiona)
.impeccable/config.local.json

# Sistema
.DS_Store
